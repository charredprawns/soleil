export default (dataType, data) => {
    switch (dataType) {
        case 'int':
          let sortedData = data.sort((a, b) =>)
          return data
        case 'double':
          break
    
        default:
          break
      }
}
